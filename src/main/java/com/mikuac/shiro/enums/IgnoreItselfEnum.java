package com.mikuac.shiro.enums;

public enum IgnoreItselfEnum {
    /**
     * 忽略自身
     */
    IGNORE_ITSELF,
    /**
     * 不忽略自身
     */
    NOT_IGNORE,
    /**
     * 只需要自身
     */
    ONLY_ITSELF
}